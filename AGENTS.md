# AGENTS.md

## Cursor Cloud specific instructions

This repository (`little_computer`) is currently an empty/newly-initialized project with only a `README.md`. There are no application services, dependencies, or build steps at this time.

### Available development tools on the VM

- **Node.js** v22 with npm, pnpm, and yarn
- **Python** 3.12 with pip
- **Git** 2.43

### Notes for future agents

- When application code is added to this repository, update the VM environment update script accordingly (e.g. `npm install`, `pip install -r requirements.txt`, etc.).
- Check `package.json`, `requirements.txt`, or similar files to determine which package manager and install commands to use.
